'use client'
// import MessageThread from "../Components/messagethread";
import MessageThread from "../(Components)/msg/messagethread";

export default function MessagePage() {
    return (
        <div className="flex flex-col h-screen">
            <MessageThread />
        </div>
    );
}